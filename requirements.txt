google-adk>=2.2.0
litellm>=1.83.7,<=1.83.14
openai>=1.100.2
langchain-openai
# Install NeMo separately on Windows; the notebook uses a lexical index instead of annoy:
# python -m pip install --no-deps nemoguardrails
